Current Work
============


BUGS
====

New Features
============

Random hues - use it to make each grenade a different hue.

Allow a single texture to contain multiple images

View clipping.

Look at the maths library for vectors.

Add more grenades, and some "solid" objects

Create a random class/helpers
	Produce floats in a specified range.
	Produce colors
	Produce Vector2s (Use interpolation method on Vector2).
	Maybe also using an "Easy" function, to spread non-linearly.


Change the spark to white, and color it with a random color.
	Color class - factory method using HSB

Create the Costume class, and use it to pick a random Pose for each of the sparks.

Use the model mat4 to allow for scaling and rotation of Actors.

Fonts
	Allow loading of multiple ranges of characters.

Tags
	

Utility Actions
	MovementXY
	MovementRotate
		Constructor takes a speed
		Also input names with default values


Load scenes from a file

Split the project into multiple modules
	core
	demo
	editor

The Resources Editor

Scene Editor

StageConstraint

New Game Wizard
	Choice of a completely blank project, or a "Hello World"
	Hello World will have a font, a Play director and a Splash director
		Splash will display some simple text and a button to lauch "level01"
		Maybe also have a "Me" Role

Code Templates
	Create one of Director, Role, Tags

